-- No-op anchor for the `headless_conversation_backfill` Rust migration
-- (see `src/store/migrations/headless_conversation_backfill.rs`).
-- SQLite mirror of the postgres anchor at the same version.

SELECT 1;
