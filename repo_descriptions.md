# Repository Descriptions

## dourolabs/metis
Primary Metis mono-repo. Contains CLI (`metis`), backend services (`metis-server`, `metis-s3`), shared crates (`metis-common`), and UIs (`metis-ui`, `metis-component-library`). Use for most agent development, deployment tooling, and automation scripts.
