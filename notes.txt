## Kubernetes setup

(requires docker to be running)

```
kind create cluster --name local-dev
```

To delete the cluster
```
kind delete cluster --name local-dev
```


## Building & Running

```cargo build``` in the top level will build all projects.


`./scripts/docker-build.sh` will build docker images for codex and metis-server, and additionally push those images into kind's image repository.

`./scripts/service.sh` lets you start/stop/restart the server inside the kubernetes cluster. Typical usage is:

`./scripts/service.sh start` 
`./scripts/service.sh status` -- this will print out a URL that you can use with the metis cli to run commands against the server


`./scripts/docker-build.sh && ./scripts/service.sh restart` -- redeploy the current version of the code to the cluster. Note the URL from status above stays the same.

`./scripts/service.sh destroy` -- tear down the services in the cluster.


## TODO items



## Important next steps

Right now you can throw change ideas into the system and it will circle back with PRs.
The bottleneck is reviewing / quality control -- the next important goal is to automate the validation process. 
Clearly, unit tests are one approach to solving this problem;
however, unit tests are always imperfect, and it will be difficult to guarantee that the unit tests for a new feature are sufficient.

How do we validate that software works in practice? We have a pipeline of validation filters:
production
staging
code review
local development environment
unit tests
build / lint 

As you go higher in the stack, the testing becomes more realistic and expensive.

So what do we need to do for the AI? We need to enable it to perform all of these types of testing.
We also need to ensure that it puts appropriate validation measures in place as it goes -- it won't do this without being pushed.
How do we validate the validation measures? Well maybe that's easier than validating the code -- we just look for tests and try the software out.
Or maybe we ask it how it knows that the code is right?

Maybe we can try prompting like:
"You are a quality control engineer. Your job is to ensure that the software being developed here is correct and bug free.
Please audit the quality control measures in this codebase and give it a grade from 1-10. Please additionally suggest some
improvements that could improve the system."

The real question here is how do we combine noisy / inaccurate parts to produce a more correct whole? 
The fundamental solution to these problems is redundancy (I think?) -- you run multiple noisy things, but then check agreement or other invariants on the joint results.

P(x = correct) = 0.9
P(y = correct) = 0.9
P(x = correct & y = correct | x == y) = 0.81 / (0.81 + 0.01) = ~0.98

but you also end up with a 0.18 probability that the results disagree. what then? Just try again!

So look one way to take advantage of this is to define a feature spec F.
Then you spawn two agents, one to write the code, and one to write the tests.
If the tests pass on the code, then the probability of correctness should be high (can we quantify this?).
The problem with this approach is that there can be minor divergences between the two implementations.

P(input)
P(x(input) == y(input) | input, x, y)
a(x, y) = sum_input P(input) * I(f(input) == y(input)) -- agreement level

Let's say the true function is f. We want to show that if a(x,y) is high, then a(f, x) is also high.
assume x and y are drawn such that P(x(input) == f(input)) == 0.9

P(a(f, x)) - binomial 
P(a(f, y)) - binomial 

eh this kind of sucks to do the math for

Maybe the simpler solution is to have a feature spec F, then have an agent generate a solution X, then ask 


--- 
Question stack:
- does the software match the spec?
- does the spec reflect the right behavior for users of the system?


## What are the experiments?

The right way to think about this is that I am building infrastructure for running a set of experiments with LLM software development.
The object of those experiments is to invent a technique/method for autonomously developing software. 
So what we need is an experimental setup -- what are the inputs and outputs? how will we measure the results? what conditions do we want to try?


## TODOs

1. Fix the deployment process, and specifically how configuration information is passed to the deployed services. E.g. there are env variables in service.sh that aren't wired up.
2. imagePullPolicy: IfNotPresent is dangerous with the latest tag. this only works due to the current local development setup where the image is pushed into kind.
3. Dockerfiles shouldn't run services as root 


Help me write unit tests for the metis-cli. As a first step, please enable dependency injection for MetisClient so that each CLI command can be passed a mock client implementation. Further implement a mock client and implement one unit test for the Spawn command using this mock
