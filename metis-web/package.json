{
  "name": "metis-web-root",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r --filter @metis/api build && pnpm -r --filter @metis/ui build && pnpm -r --filter @metis/web build",
    "dev": "pnpm -r --filter @metis/web dev",
    "dev:mock-server": "pnpm -r --filter @metis/mock-server dev",
    "dev:server": "pnpm -r --filter @metis/web dev:server",
    "dev:test": "./scripts/dev-test.sh",
    "e2e": "pnpm --filter @metis/web exec playwright test",
    "generate-types": "cd .. && TS_RS_EXPORT_DIR=metis-web/packages/api/src/generated cargo test -p metis-common --features ts export_bindings && cd metis-web && prettier --write packages/api/src/generated/",
    "lint": "eslint packages/*/src --ext .ts,.tsx",
    "test": "vitest run",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-react-hooks": "^5.0.0",
    "eslint-plugin-react-refresh": "^0.4.0",
    "prettier": "^3.4.0",
    "typescript": "^5.7.0",
    "jsdom": "^25.0.0",
    "vitest": "^2.1.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
