[package]
name = "metis-build-cache"
version = "0.1.0"
edition = "2024"

[dependencies]
metis-common = { path = "../metis-common" }
thiserror = "1.0"
globset = "0.4"
walkdir = "2"
tar = "0.4"
zstd = "0.13"
tokio = { version = "1", features = ["rt", "sync"] }

[dev-dependencies]
tempfile = "3"
